# Platform Zero

Platform Zero maintains a modular, Podman-based self-hosted services platform.
Each `*-stack-module` repository owns one deployable slice; `sso-stack-generator`
composes exact module pins into immutable release bundles.

Repository-wide contribution, security, dependency, and CI policy lives here.
